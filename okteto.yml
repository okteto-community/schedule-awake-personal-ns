build: 
  wake: 
    context: .
    dockerfile: Dockerfile

deploy: 
  commands: 
  - kubectl delete cronjob -n ${OKTETO_WAKE_NAMESPACE} wakeup-namespace-cronjob-${OKTETO_USERNAME} --ignore-not-found --wait
  - envsubst < cronjob.yaml | kubectl apply -f -


